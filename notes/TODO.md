# TODO

- [ ] `leaf` resource and data source
- [ ] `leaves` data source
- [ ] insert a document version marker into entity records. Use this version when un/marshalling data. Allow callers to read this marker. Consider allowing callers to write their own version marker (outside of Attributes)
- [ ] allow attributes to be `N` (number)
- [ ] allow attributes to be `BOOL` (boolean)
- [ ] allow attributes to be `M` (map)
- [ ] fix acceptance tests
